kind: Service
apiVersion: v1
metadata:
  name: cv3am-ambulance-webapi
spec:
  selector:
    pod: cv3am-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port
